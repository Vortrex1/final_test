name: Unit & Integration Tests

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - main

env:
  DOTNET_VERSION: '8.0.x'
  TEST_PROJECT: HotelBooking.Tests/HotelBooking.Tests.csproj

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
      
      - name: Restore NuGet packages
        run: dotnet restore ${{ env.TEST_PROJECT }}
      
      - name: Build project
        run: dotnet build ${{ env.TEST_PROJECT }} --configuration Release --no-restore
      
      - name: Run Unit Tests
        run: dotnet test ${{ env.TEST_PROJECT }} --filter "FullyQualifiedName~HotelBooking.Tests.Unit|FullyQualifiedName~HotelBooking.Tests.UnitTest1" --configuration Release --no-build --logger "trx;LogFileName=unit-tests.trx" --collect:"XPlat Code Coverage"
      
      - name: Run Integration Tests
        run: dotnet test ${{ env.TEST_PROJECT }} --filter "FullyQualifiedName~HotelBooking.Tests.Integration" --configuration Release --no-build --logger "trx;LogFileName=integration-tests.trx"
      
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: |
            **/TestResults/*.trx
          retention-days: 30
          if-no-files-found: ignore

  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
      
      - name: Restore NuGet packages
        run: dotnet restore ${{ env.TEST_PROJECT }}
      
      - name: Build project
        run: dotnet build ${{ env.TEST_PROJECT }} --configuration Release --no-restore
